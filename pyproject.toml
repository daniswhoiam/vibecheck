[project]
name = "vibecheck"
version = "0.1.0"
description = "Vibecheck monorepo"
requires-python = ">=3.13"

[tool.uv.workspace]
members = [
    "lib/db",
    "services/ingestion",
    "services/worker",
    "services/api",
]
