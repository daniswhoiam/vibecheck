# API Configuration
# Base URL for the backend API (FastAPI running on port 8000)
VITE_API_BASE_URL=http://localhost:8000

# Note: In development, the frontend (port 8080) proxies API requests
# to the backend (port 8000) to avoid CORS issues.
# See vite.config.ts for proxy configuration.
