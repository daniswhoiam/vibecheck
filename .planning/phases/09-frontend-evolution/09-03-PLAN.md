---
phase: 09-frontend-evolution
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/components/SourceFilterToggle.tsx
autonomous: true
requirements: [FRON-01]

must_haves:
  truths:
    - "SourceFilterToggle renders 5 pill-style toggle buttons: All Sources, HN, Reddit, Discourse, Dev.to"
    - "Selecting a source calls onChange with the source id string"
    - "Active state is visually distinct (data-state=on on the active ToggleGroupItem)"
    - "Component uses @radix-ui/react-toggle-group with type=single for keyboard nav and ARIA compliance"
    - "SourceFilterToggle.test.tsx tests all pass GREEN"
  artifacts:
    - path: "src/components/SourceFilterToggle.tsx"
      provides: "Pill-style segmented toggle button group for source filtering"
      exports: ["SourceFilterToggle"]
      min_lines: 40
  key_links:
    - from: "src/components/SourceFilterToggle.tsx"
      to: "src/components/ui/toggle-group.tsx"
      via: "ToggleGroup, ToggleGroupItem import"
      pattern: "import.*ToggleGroup.*toggle-group"
---

<objective>
Build the SourceFilterToggle component — a pill-style segmented toggle button group for selecting which data source to view on the entity detail page.

Purpose: Implements FRON-01's source filter UI. The component is self-contained and can be tested and built independently of the Detail page integration.
Output: src/components/SourceFilterToggle.tsx with passing unit tests.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-frontend-evolution/09-CONTEXT.md
@.planning/phases/09-frontend-evolution/09-RESEARCH.md
@.planning/phases/09-frontend-evolution/09-02-SUMMARY.md
@src/types/api.ts
@src/components/ui/toggle-group.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SourceFilterToggle component and make unit tests GREEN</name>
  <files>src/components/SourceFilterToggle.tsx</files>
  <action>
Create `src/components/SourceFilterToggle.tsx`.

**Locked design decisions (from CONTEXT.md — do not deviate):**
- Segmented toggle button group (pill-style)
- 5 options: "All Sources", "HN", "Reddit", "Discourse", "Dev.to"
- "All Sources" default selected state
- Single-select (type="single")
- Active state: filled background via data-state="on"
- Use @radix-ui/react-toggle-group via shadcn wrapper (src/components/ui/toggle-group.tsx)

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group";
import type { SourceFilter } from "@/types/api";

interface SourceFilterToggleProps {
  value: SourceFilter;
  onChange: (value: SourceFilter) => void;
  className?: string;
}

const SOURCES: { id: SourceFilter; label: string }[] = [
  { id: "all", label: "All Sources" },
  { id: "hn",  label: "HN" },
  { id: "reddit",    label: "Reddit" },
  { id: "discourse", label: "Discourse" },
  { id: "devto",     label: "Dev.to" },
];

export function SourceFilterToggle({ value, onChange, className }: SourceFilterToggleProps) {
  const handleChange = (newValue: string) => {
    // Radix ToggleGroup type="single" sends empty string when deselecting the active item.
    // Treat empty string as "all" to prevent deselecting everything.
    if (!newValue) return;
    onChange(newValue as SourceFilter);
  };

  return (
    <div className={className}>
      <ToggleGroup
        type="single"
        value={value}
        onValueChange={handleChange}
        className="flex flex-wrap gap-2"
        aria-label="Filter by data source"
      >
        {SOURCES.map((source) => (
          <ToggleGroupItem
            key={source.id}
            value={source.id}
            className="px-4 py-1.5 rounded-full text-sm font-medium data-[state=on]:bg-foreground data-[state=on]:text-background"
            aria-label={`Filter to ${source.label}`}
          >
            {source.label}
          </ToggleGroupItem>
        ))}
      </ToggleGroup>
    </div>
  );
}
```

After creating the file, run `npm test -- src/components/SourceFilterToggle.test.tsx` and fix any failures. Common issue: shadcn's ToggleGroup may use a different import path or className approach — adjust to match what's in `src/components/ui/toggle-group.tsx`.

The test checks `data-state="on"` on the active button — this is standard Radix behavior with `type="single"`.
  </action>
  <verify>
    <automated>cd /Users/daniswhoiam/Projects/vibecheck && npm test -- src/components/SourceFilterToggle.test.tsx 2>&1 | tail -20</automated>
    <manual>Verify component exports SourceFilterToggle; all 3 unit tests pass; uses ToggleGroup from ui/toggle-group.tsx</manual>
  </verify>
  <done>All SourceFilterToggle.test.tsx tests pass GREEN; component file exists and exports SourceFilterToggle named export</done>
</task>

</tasks>

<verification>
`npm test -- src/components/SourceFilterToggle.test.tsx` — 3/3 tests GREEN.
`npx tsc --noEmit` — no TypeScript errors in SourceFilterToggle.tsx.
</verification>

<success_criteria>
- src/components/SourceFilterToggle.tsx exports SourceFilterToggle
- Uses ToggleGroup type="single" with 5 options
- Handles empty-string deselect by keeping current value (no deselecting "all")
- SourceFilter type imported from @/types/api
- All 3 SourceFilterToggle unit tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-evolution/09-03-SUMMARY.md`
</output>
