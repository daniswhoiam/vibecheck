---
phase: 09-frontend-evolution
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified: []
autonomous: false
requirements: [FRON-01, FRON-02]

must_haves:
  truths:
    - "User can see the source filter toggle with 5 pills on the entity detail page"
    - "Selecting a source filter updates the visible data sections"
    - "Sentiment by Aspect section is visible below the trend chart"
    - "Aspect bars render for entities with aspect data; empty state shows for entities without"
    - "Existing entity list and sentiment trend views work with no visible regressions"
    - "Source filter selection is persisted in URL (?source=reddit) after reload"
  artifacts:
    - path: "src/pages/Detail.tsx"
      provides: "Complete entity detail page with source filter and aspect chart"
      contains: "SourceFilterToggle"
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 9 frontend feature by the user.

Purpose: Automated tests verify component behavior in isolation. This checkpoint confirms the features work end-to-end in the running application with real data (or realistic mock data). Catches visual regressions and interaction issues that tests cannot cover.
Output: User approval of the Phase 9 UI experience.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-frontend-evolution/09-CONTEXT.md
@.planning/phases/09-frontend-evolution/09-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 9 frontend evolution:
- Source filter toggle (pill-style, 5 options) on entity detail page below the header
- Source filter persists in URL (?source=reddit) and updates mentions section
- "Sentiment by Aspect" section with horizontal diverging bar chart below the trend chart
- Skeleton loading states during data fetches
- Empty state handling when no aspect data exists for a source
  </what-built>
  <how-to-verify>
Start the development server:
```bash
cd /Users/daniswhoiam/Projects/vibecheck && npm run dev
```
Visit http://localhost:5173 in a browser.

**Check 1: Source filter appears (FRON-01)**
1. Click on any entity (tool or LLM) to open its detail page
2. Verify you see 5 pills below the entity header: "All Sources", "HN", "Reddit", "Discourse", "Dev.to"
3. Click "Reddit" — URL should update to include `?source=reddit`
4. Reload the page — "Reddit" pill should still be selected
5. Click "All Sources" — URL source param should disappear

**Check 2: Source filter affects mentions section**
1. On entity detail page with "All Sources" selected, note how many mentions are visible
2. Click "Discourse" — verify the recent mentions section shows only Discourse posts (or empty state if none)
3. The empty state should say "No Discourse data indexed yet" if no posts exist for that source

**Check 3: Sentiment by Aspect section (FRON-02)**
1. Scroll down on the entity detail page past the trend chart
2. Verify a "Sentiment by Aspect" card is present
3. If aspect data exists: verify horizontal bars are visible for aspects, sorted strongest-to-weakest
4. If no aspect data: verify empty/skeleton state (the LLM pipeline may not have run yet in dev)
5. Verify aspect names use title case ("Code Quality" not "code_quality")
6. Verify the color scheme: positive bars are teal/green, negative bars are amber

**Check 4: No regressions**
1. Return to the entity list (/ route) — verify tools list still loads normally
2. Verify tool cards still show mini sparklines
3. Open 2-3 different entity detail pages — all should load without JS errors in browser console

**Check 5: Trend chart clean**
1. On any entity detail page, verify the trend chart shows a single aggregate line (not the old multi-line News/Reddit view)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass. Or describe any issues found so they can be fixed before phase completion.</resume-signal>
</task>

</tasks>

<verification>
User-approved visual and functional verification.
</verification>

<success_criteria>
- Source filter toggle visible and functional on entity detail page
- URL persistence (?source=) confirmed working
- Sentiment by Aspect section visible and correctly styled
- No JS console errors on any page
- Entity list and tool cards unaffected (no regressions)
- User types "approved"
</success_criteria>

<output>
After user approval, create `.planning/phases/09-frontend-evolution/09-06-SUMMARY.md`
</output>
