---
phase: 03.1-entity-seeding-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/scripts/seed_entities.py
autonomous: true

must_haves:
  truths:
    - "Entity seeding runs automatically on FastAPI startup"
    - "Seeding completes before scheduler starts processing jobs"
    - "Startup logs confirm entity seeding success with entity count"
    - "Jobs can immediately process articles without manual intervention"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI lifespan with entity seeding integration"
      contains: "seed_entities()"
      min_lines: 95
    - path: "backend/scripts/seed_entities.py"
      provides: "Reusable entity seeding function"
      exports: ["seed_entities"]
      min_lines: 130
  key_links:
    - from: "backend/main.py"
      to: "backend/scripts/seed_entities.py"
      via: "import seed_entities in lifespan startup"
      pattern: "from scripts.seed_entities import seed_entities"
    - from: "backend/main.py"
      to: "pipeline.scheduler"
      via: "startup sequence ordering (seeding before scheduler.start())"
      pattern: "await seed_entities\\(\\).*scheduler\\.start\\(\\)"
---

<objective>
Integrate entity seeding into FastAPI startup to enable full E2E article ingestion without manual intervention.

Purpose: Close critical gap identified in v1-MILESTONE-AUDIT.md where the existing seed_entities.py script functions correctly but is never called automatically, breaking the article ingestion flow due to foreign key constraints.

Output: Automated entity seeding that runs on application startup before the scheduler begins processing jobs, with proper logging and error handling.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-entity-seeding-automation/03.1-RESEARCH.md
@.planning/v1-MILESTONE-AUDIT.md

@backend/main.py
@backend/scripts/seed_entities.py
@backend/pipeline/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Refactor seed_entities.py for lifespan integration</name>
  <files>backend/scripts/seed_entities.py</files>
  <action>
Modify the seed_entities() function to support both standalone execution and lifespan context integration:

1. Keep the standalone async def seed_entities() function intact (for CLI use)
2. Add logging that works in both contexts (print() is fine - used elsewhere in main.py)
3. Ensure the function handles idempotency correctly (already implemented - checks existing entities)
4. Return the count of seeded entities for logging in lifespan
5. Keep all existing error handling and transaction logic

DO NOT change the function signature or break standalone CLI execution. The script must still work via `python scripts/seed_entities.py`.
  </action>
  <verify>grep -n "async def seed_entities" backend/scripts/seed_entities.py shows function exists and returns entity count</verify>
  <done>seed_entities() function can be imported and called from other modules while remaining executable as a standalone script</done>
</task>

<task type="auto">
  <name>Integrate seed_entities into FastAPI lifespan</name>
  <files>backend/main.py</files>
  <action>
Update the @asynccontextmanager lifespan() function in main.py to seed entities before starting the scheduler:

1. Add import: `from scripts.seed_entities import seed_entities`
2. In lifespan startup, AFTER run_migrations() and BEFORE setup_jobs():
   - Add print() log: "Seeding entities..."
   - Call await seed_entities() (wrap in try/except)
   - On success: print entity count confirmation
   - On failure: print error and re-raise to fail startup (seeding is fatal)
3. Ensure proper startup sequence: migrations → seeding → scheduler

The seeding MUST run before scheduler.start() to prevent jobs from attempting to process articles with non-existent entity_id foreign keys.
  </action>
  <verify>grep -A 5 "await seed_entities" backend/main.py shows seeding called before scheduler.start()</verify>
  <done>FastAPI lifespan executes entity seeding before scheduler initialization, maintaining correct dependency order</done>
</task>

<task type="auto">
  <name>Verify startup sequence and logging</name>
  <files>backend/main.py</files>
  <action>
Confirm the complete startup sequence in lifespan() function:

Order must be:
1. print("Starting up application...")
2. run_migrations()
3. await seed_entities() with try/except
4. setup_jobs()
5. scheduler.start()
6. yield

Add logging after seeding:
- Success: print "Entity seeding completed successfully" or similar
- Include entity count (reuse script's existing output)

Ensure failure handling:
- If seeding fails: log error and raise (fail startup)
- Do not continue to scheduler.start() if seeding fails
  </action>
  <verify>grep -B 2 -A 8 "async def lifespan" backend/main.py shows migrations → seeding → scheduler sequence</verify>
  <done>Startup logs show complete initialization sequence with entity seeding confirmation before APScheduler starts</done>
</task>

</tasks>

<verification>
After implementation, verify the complete flow:

1. **Startup verification:**
   - `docker-compose up` shows migration completion
   - Entity seeding executes before scheduler
   - Logs confirm "Successfully seeded X entities" (or "All curated entities already exist")
   - Scheduler starts after seeding completes

2. **API verification:**
   - GET /entities returns 10 entities immediately after startup (no manual seeding required)
   - All entities have correct names and categories from CURATED_ENTITIES

3. **Job verification:**
   - News and story jobs can execute without foreign key constraint violations
   - Articles can be stored with valid entity_id references
   - No "entity does not exist" errors in job logs

4. **Idempotency verification:**
   - Restarting FastAPI does not create duplicate entities
   - Script handles existing entities gracefully
</verification>

<success_criteria>
Entity seeding automation is complete when:

1. ✅ FastAPI startup automatically seeds entities without manual intervention
2. ✅ Startup logs confirm seeding success with entity count
3. ✅ Scheduler starts only after seeding completes successfully
4. ✅ Jobs can process articles immediately after first startup
5. ✅ GET /entities returns 10 entities on fresh database
6. ✅ Restarting application does not create duplicate entities (idempotent)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-entity-seeding-automation/03.1-01-SUMMARY.md`
</output>
